const auth = require('./auth.controller')
const user = require('./users.controller')
const image = require('./Image.controller')
const note = require('./Image.controller')

module.exports = {
    auth,
    user,
    image,
    note,
}